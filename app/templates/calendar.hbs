<h1>Caitlyn, Good Morning!</h1>

{{#each model as |calendar| }}
	{{#each calendar.cues as |cue|}}
	  {{sub-cue cue=cue}}
	  {{!-- <h4>{{cue.title}}</h4>
	  <p>{{cue.text}}</p>
	  <br />

	  {{#each cue.subCues as |subCue|}}
	    <h4>{{subCue.title}}</h4>
	  {{/each}} --}}
	{{/each}}
{{/each}}
{{outlet}}